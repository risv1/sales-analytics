export const root = {
  getSalesByProductCategory: (_: any, { filter }: { filter: { category: string } }) => {
    return [];
  },
  getSalesDataByProductStock: (
    _: any,
    { threshold }: { threshold: number }
  ) => {
    return [];
  },
};
